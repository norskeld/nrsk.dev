---
import ArticleList from '@/screens/blog/ArticleList.astro'
import BlogLayout from '@/layouts/Blog.astro'

import { withSeoOptions, Common } from '@/site.config'

const props = withSeoOptions({
  title: `Blog – ${Common.title}`,
  description: 'Articles and notes about software development.',
  openGraph: {
    basic: {
      title: 'Blog',
      type: 'website',
      url: Astro.url.href
    }
  }
})
---

<BlogLayout {...props}>
  <ArticleList />
</BlogLayout>
